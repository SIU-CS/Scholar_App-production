<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/activity_profile"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="siucs.scholarsprogramapp.ProfileActivity">

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <TextView
            android:text="TextView"
            android:textAlignment="center"
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:textAppearance="?android:attr/textAppearanceLarge"
            android:layout_gravity="center_horizontal"
            android:id="@+id/textViewWelcome" />

        <LinearLayout
            android:orientation="horizontal"
            android:layout_width="match_parent"
            android:layout_height="180dp">

            <Button
                android:text="Contact List"
                android:drawableTop="@drawable/ic_contact"
                android:id="@+id/buttonContactList"
                style="@style/menu_buttons" />

            <Button
                android:text="Service Hours"
                android:drawableTop="@drawable/ic_service_hours"
                android:id="@+id/buttonServiceHours"
                style="@style/menu_buttons" />
        </LinearLayout>

        <LinearLayout
            android:orientation="horizontal"
            android:layout_width="match_parent"
            android:layout_height="180dp">

            <Button
                android:text="Calendar"
                android:drawableTop="@drawable/ic_calendar"
                android:id="@+id/buttonCalendar"
                style="@style/menu_buttons" />

            <Button
                android:text="Message Board"
                android:drawableTop="@drawable/ic_message_board"
                android:id="@+id/buttonMessageBoard"
                style="@style/menu_buttons" />

        </LinearLayout>

        <Button
            android:text="Log Out"
            android:drawableStart="@drawable/ic_logout"
            android:layout_height="100dp"
            android:drawablePadding="-80dp"
            android:id="@+id/buttonLogOut"
            style="@style/menu_buttons" />
    </LinearLayout>

</RelativeLayout>
